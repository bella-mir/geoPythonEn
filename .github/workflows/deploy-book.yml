name: Deploy the book to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# one deployment at a time, and let a running one finish
concurrency:
  group: pages
  cancel-in-progress: false

env:
  # the site lives under https://<user>.github.io/geoPythonEn/
  BASE_URL: /geoPythonEn

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync

      # MyST stores executed notebooks in _build/execute and only re-runs the ones
      # whose code changed; `cache/` holds the OpenStreetMap responses osmnx downloads
      - name: Restore the execution cache
        uses: actions/cache@v4
        with:
          path: |
            _build/execute
            _build/cache
            cache
          key: myst-execute-${{ github.sha }}
          restore-keys: myst-execute-

      - name: Build the book
        # --execute runs the notebooks, so the maps and tables end up in the pages
        run: uv run myst build --html --execute

      - name: Upload the site
        uses: actions/upload-pages-artifact@v5
        with:
          path: _build/html

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
